"""
Базовый класс шаблона сообщения.

LogTemplate — абстрактный предок для конкретных шаблонов логов.
Наследники реализуют render(**kwargs) -> str.
"""


class LogTemplate:
    """Базовый шаблон сообщения.

    Роль: единый контракт для всех шаблонов. Наследники
          получают параметры через kwargs и возвращают готовую
          строку для логирования.
    """

    def render(self, **kwargs) -> str:
        """Рендерит сообщение.

        Вход: **kwargs — параметры шаблона.
        Выход: строка.
        Роль: абстрактный. Наследник обязан переопределить.
        """
        raise NotImplementedError(
            f"{type(self).__name__} должен реализовать render()"
        )